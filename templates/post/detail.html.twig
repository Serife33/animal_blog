{% extends 'base.html.twig' %}

{% block body %}

<h2>{{post.title}}</h2>
    
    <p>Théme : {{post.theme.name}}</p>
    <p>{{post.content}}</p>
    <p>{{post.image}}</p>
    

  <button><a href="{{ path('app_post')}}">Retour posts</a></button>

{% endblock %}